{% extends 'master' %}

{% block content %}

<h2>{{post.title}}</h2>

<small>{{post.created_at}}</small>
<div class="image">
        <img src="{{post.image}}" syyle="width: 100%; heigth: auto;">
    </div>
<p>{{post.body}}</p>

{% endblock %}